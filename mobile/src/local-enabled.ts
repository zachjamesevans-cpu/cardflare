/**
 * Whether the Local tab is on - the app's copy of the website's
 * src/lib/local/enabled.ts, kept identical on purpose and held equal by
 * tests/unit/local-enabled.test.ts. Read that file for the why.
 *
 * Off: the Room tab is back in the bar, Local is hidden, and the
 * conversations people already had stay readable under Messages.
 */
export const LOCAL_ENABLED = false;

/**
 * A showcase note's ceiling, on its own so the client editor can import
 * it without dragging the server-only profile module into the bundle.
 * `profile.ts` re-exports it as the number the write enforces.
 */
export const SHOWCASE_NOTE_MAX = 140;
